Dockerfile
.dockerignore
node_modules
npm-debug.log
README.md
.next
.git
.gitignore

# secrets / local config — never bake into an image layer
.env
.env.*
certs
*.pem
.deploy-state.json

# generated at build time (prisma generate during `npm run build`)
/src/generated

# downloaded model weights
/.cache

# deployment assets the app image doesn't need (built from their own contexts)
edge
updater
Caddyfile
compose.yaml
compose.prod.yaml
bootstrap.sh

# test artifacts
/e2e/.auth
/e2e/*.png
/test-results
/playwright-report
/blob-report
