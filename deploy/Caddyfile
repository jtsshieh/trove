# Edge reverse proxy for the LAN deployment. Terminates TLS with the mkcert cert
# for trove.local and forwards to the Next.js app on the internal network.
trove.local {
	tls /certs/trove.local.pem /certs/trove.local-key.pem
	reverse_proxy app:3000
}
